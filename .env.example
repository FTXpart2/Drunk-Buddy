# --- core ---
ANTHROPIC_API_KEY=
ANTHROPIC_MODEL=claude-haiku-4-5

# channel: "local" (terminal chat) or "bluebubbles" (live iMessage)
CHANNEL=local
PORT=8787

# --- state (optional; falls back to in-memory if unset) ---
REDIS_URL=

# --- BlueBubbles gateway (Phase 0) ---
BLUEBUBBLES_SERVER_URL=
BLUEBUBBLES_PASSWORD=
BLUEBUBBLES_METHOD=apple-script
# public https url of THIS backend (ngrok), used by channel:register
PUBLIC_URL=

# --- heart-rate guardian (Apple Watch -> /watch link -> /vitals) ---
# secret that signs the one-tap health link token (set anything random in prod)
HEALTH_LINK_SECRET=
# bpm thresholds that trigger a proactive check-in while party mode is on
HR_LOW=45
HR_HIGH=130
# how often the heartbeat re-checks open concerns (ms)
HEARTBEAT_MS=30000
# silence after a check-in before escalating to emergency contacts (ms)
ESCALATE_AFTER_MS=300000

# --- sponsors (later phases) ---
# voice notes: Deepgram does both STT (transcribe inbound) and Aura TTS (speak replies).
# DEEPGRAM_TTS_MODEL: an Aura voice (defaults to aura-2-orion-en, masculine).
DEEPGRAM_API_KEY=
DEEPGRAM_TTS_MODEL=
BROWSERBASE_API_KEY=
BROWSERBASE_PROJECT_ID=
# The Token Company: ML prompt compression. Set this (a "ttc-..." key) to wrap the
# Anthropic client with withCompression — system + user messages are compressed
# before each Claude call. Uses your own ANTHROPIC_API_KEY for the actual call.
TOKEN_COMPANY_API_KEY=
ARIZE_API_KEY=
ARIZE_SPACE_ID=
